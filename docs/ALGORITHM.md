# Munch — Recommendation Algorithm

> Phase 0: design contract only. Implementation lands in Phase 2 (layers 1–3)
> and Phase 5 (layers 4–5). **Every** algorithm change after that appends an
> entry to the iteration log below with before/after metrics — that paper
> trail is part of the deliverable.

## Layered design (spec §7, amended per DECISIONS.md D-007…D-010)

| Layer | What | When it works |
|---|---|---|
| 1 | Hard filters: metro, open-now (3-state), dietary, price ceiling (permissive on imputed), range | Always; pure functions, <50ms |
| 2 | Heuristic weighted-sum scorer; weights in config, never inline | From swipe #1 (anchors drive cold start) |
| 3 | Offline eval harness on synthetic users — **plumbing validation, not lift evidence** (D-007) | Phase 2 onward |
| 4 | LightGBM LambdaMART (group = served deck, label_gain [0,1,5], unseen excluded, position logged — D-008) | Gated: ≥50k real swipes / ≥5k mixed-label decks |
| 5 | Reason text: templated default; Haiku pre-generated + cached, off the serve path (D-004) | Phase 5, feature-flagged |

Deck assembly on top of scoring: Thompson sampling on `internal_score`,
2/10 exploration slots, ≤3 cards per cuisine per deck, `explore` flag logged
per impression (D-009).

## Cold start

Anchor restaurants → hand-built, versioned cuisine adjacency map over the
canonical cuisine nodes (`CANONICAL_CUISINES`, ~50 nodes derived from the edge
list — the source of truth; raw OSM tags normalized at seed time), three weight
tiers (1.0 self / 0.6 near / 0.3 related). Anchors also initialize price and
geography priors. No embeddings in v1 — nothing to train them from; upgrade
path is swipe-derived co-preference (PMI), data-gated.

**Known limitation (honest):** `cuisine_affinity` (0.40) and
`swipe_pattern_match` (0.25) are both cuisine-overlap signals over the same
adjacency map, differing only in source (static anchors vs recency-decayed
recent right-swipes). They are ~0.5 correlated by construction, so the 0.25 is
**not** independent lift — much of it re-weights cuisine the 0.40 already
counts, and the synthetic eval cannot separate them. Spec weights are kept
as-is for v1; redefining `swipe_pattern_match` as a residual over the static
prior (or onto an orthogonal signal) is a real-data tuning task, not a
synthetic one.

## internal_score

Beta posterior mean, prior strength ≈20 effective impressions centered on the
live global right-swipe rate (recomputed weekly); conversions add
pseudo-successes; ~90-day half-life decay. Surfaced in UI as the
"N% of people who like what you like, liked this" number. 100% Munch-owned
data; never a scraped third-party rating. **Phase 2 status:** the scorer
prefers a persisted `internal_score` when present, else recomputes the Beta
posterior live from raw counts; the ~90-day decay is applied by the Phase-3
weekly recompute job that writes `internal_score`, not live at request time.

## Evaluation protocol

- Metrics: Precision@k, Recall@k, NDCG@k, MRR, hit-rate@k for k ∈ {1,3,5}.
- Every run reports against a **random-ranker floor** and an **idealized oracle
  ceiling** (ranks by the clean true-utility signal, so a definitional 1.0);
  models are judged by position in that bracket, never by closeness to the
  oracle (D-020).
- Simulator hides a `vibe` signal and exposes only a thin taste projection to
  the ranker (anchors + adjacency, not the full per-cuisine utility vector);
  binary labels carry 15% observation noise. Assumptions live in simulator.py.
- Champion/challenger: a challenger must beat the champion on NDCG@5 by a
  configured margin to pass *plumbing* regression. **Promotion to production
  champion additionally requires real-data evidence** (interleaving/A-B on
  logged swipes). The heuristic is the designated champion until the Layer-4
  data gate is met.
- Reproducible entry point: `python -m evalharness.run` (prints the metrics
  table and appends it here).

## Prior-art sources

(Per spec §7.7 — permissive licenses only, approach over copy-paste. Populated
as references are actually used, with URL + license.)

| Source | License | Used for |
|---|---|---|
| _none yet_ | | |

## Synthetic baseline (auto-generated)

The block below is rewritten by `python -m evalharness.run`. It is **plumbing
validation**, not real-world lift (D-007 / D-020). NDCG is graded against the
clean true-utility signal; the oracle ranks by that signal, so it is 1.0 by
definition — an idealized perfect-knowledge ceiling, not a target. The heuristic
sits below it because it sees only a thin projection of taste (anchors +
adjacency, not each user's full per-cuisine vector) plus a hidden `vibe` term,
and the binary labels carry 15% observation noise. Read it as "random floor <
heuristic < idealized ceiling," never as lift.

<!-- EVAL:START -->
_Generated by `python -m evalharness.run` (seed 20260611, 400 users, 300 venues, synthetic). Plumbing validation only — not real-world lift (D-007)._

| Ranker | NDCG@1 | NDCG@3 | NDCG@5 | P@5 | Recall@5 | Hit@5 | MRR |
|---|---|---|---|---|---|---|---|
| random | 0.519 | 0.559 | 0.592 | 0.346 | 0.120 | 0.887 | 0.581 |
| heuristic | 0.897 | 0.885 | 0.866 | 0.619 | 0.217 | 0.993 | 0.904 |
| oracle | 1.000 | 1.000 | 1.000 | 0.857 | 0.302 | 1.000 | 0.920 |
<!-- EVAL:END -->

## Iteration log

| Date | Change | NDCG@5 (heuristic) | Notes |
|---|---|---|---|
| 2026-06-11 | Phase 2 heuristic baseline + eval harness | 0.866 (synthetic) | First champion; learned ranker (Phase 5) must beat this on REAL swipes, not synthetic. |
| 2026-06-12 | D-020 honesty fix: grade NDCG vs clean truth (was noisy draw) | 0.866 | Oracle now a definitional ceiling; gap attributed to feature projection + label noise, not `vibe`. |
